use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8080
  secret_key: "change-this-local-compose-secret"
  limiter: false
  image_proxy: false

search:
  formats:
    - html
    - json
  safe_search: 0

outgoing:
  request_timeout: 5.0
