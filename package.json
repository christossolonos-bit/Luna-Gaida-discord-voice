{
  "name": "giada-companion",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/desktop",
    "apps/server",
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @giada/server & npm run tauri:dev --workspace @giada/desktop",
    "build": "npm run build --workspace @giada/server && npm run build --workspace @giada/web && npm run build --workspace @giada/desktop",
    "test": "npm run test --workspace @giada/server && npm run test:rust --workspace @giada/desktop",
    "lint": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
